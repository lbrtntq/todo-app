import React from 'react'

export default function About() {
    return (
        <div style={{textAlign:'center', padding:'60px'}}>
            <h1>About</h1>
            <p>
                Basic implementation of react's state props and route.<br/>
                Uses <a style={{color:'blue', textDecoration:'underline'}}href="https://jsonplaceholder.typicode.com/" target="#">jsonplaceholder</a> to get data.
            </p>
        </div>
    )
}
